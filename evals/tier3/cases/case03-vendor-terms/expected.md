MUST: proposal neutralizes runtime-specific tool names (e.g. "run the linter
  in a shell", "fetch the style guide") and notes each change made.
MUST: proposal body passes find_vendor_terms verbatim.
MUST-NOT: proposal presented as enroll-ready while still containing vendor terms.
